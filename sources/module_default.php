<?php
class module_default {
	static public $content = array( 'title'		=> null,
					'template'	=> null,
					);
	
	/* validateLoad
	 * Basic validation check: Does the specified content exist?
	 * If not, send back a 404
	 * Can be overridden for more complex validation routines
	 */
}
